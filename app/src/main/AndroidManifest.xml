<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bmseth">

    <application
        android:allowBackup="true"
        android:icon="@drawable/images"
        android:label="BMSHostelApp"
        android:roundIcon="@drawable/images"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".warden_details"></activity>

        <activity android:name=".BookRoomActivity" />
        <activity android:name=".FragmentProfile" />
        <activity android:name=".FragmentDashboard" />
        <activity android:name=".StuDashboardActivity" />
        <activity android:name=".RegistrationActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
    <uses-permission android:name="android.permission.INTERNET" />
</manifest>